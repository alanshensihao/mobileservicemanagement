public class Menu
{
  // These enum values should be used in the future for switch case in server
  public enum MenuOption
  {
    ADD_USER,
    ADD_USERS,
    UPDATE_USER,
    DELETE_USER,
    DELETE_USERS,
  }
}
